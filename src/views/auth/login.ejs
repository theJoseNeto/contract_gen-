<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            overflow-y: hidden;
            font-family: 'San Francisco', 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #F3F1E2;
        }

        .container-principal {
            display: flex;
        }



        .imagecontainer img {
            width: 150%;
        }

        .formContainer {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            width: 100%;
        }

        form {
            width: 45%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            max-width: 500px;
            margin: 50px auto;
            background-color: #fff;
            padding: 50px;
            border-radius: 12px;
            box-shadow: 0 15px 20px rgba(0, 0, 0, 0.1);
        }

        input[type="email"],
        input[type="password"],
        input[type="submit"] {
            width: 100%;
            padding: 12px;
            margin: 8px 0;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 25px;
        }

        input[type="submit"] {
            background-color: #2a2a2a;
            color: white;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #2a2a2a;
        }


        .container-error-message {
            width: 100%;
            height: 25px;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;

        }

        .error-message {
            width: 100%;
            font-size: 16px;
            font-weight: bold;
            color: #FF3B30;
            animation: shake 0.2s ease-in-out .5s;
        }

        @keyframes shake {

            0%,
            100% {
                transform: translateX(0);
            }

            25%,
            75% {
                transform: translateX(-5px);
            }

            50% {
                transform: translateX(5px);
            }
        }
    </style>

</head>

<body>

    <main class="container-principal">

        <div class="imagecontainer">
            <img src="/images/civilbuild.png" alt="Minha Imagem">

        </div>
        <div class="formContainer">
            <form action="/signin" method="post">
                <input type="email" name="email" id="" placeholder="Email cadastrado">
                <input type="password" name="password" id="" placeholder="Senha">
                <input type="submit" value="Entrar">

                <div class="container-error-message">
                    <% if (message) { %>
                        <div class="error-message">
                            <%= message %>
                        </div>
                        <% } %>

                </div>
            </form>
        </div>
    </main>
</body>

</html>